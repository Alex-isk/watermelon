import 'package:flutter/material.dart';

class WatermelonColors {

  WatermelonColors._();

  static const greenText = Color.fromRGBO(255, 255, 115, 115);
  static const blackText = Color.fromRGBO(0, 0, 0, 0.87);
  static const backgroundGreen = Color.fromRGBO(3, 136, 0, 1);
  static const red = Color(0xFFEF5350);
  static const red2 = Color(0xFFE57373);
  static const colorIcon = Color(0x8AFFFFFF);
  static const sweetText = Colors.white;
  static const infoTextBackground = Color(0xFFDBF3B8);
  // static const greyButton = Color.fromRGBO(0, 0, 0, 0.38);
  // static const blueButton = Color.fromRGBO(28, 121, 206, 1);
  // static const blackButton = Color.fromRGBO(0, 0, 0, 0.87);
  //
  // static const darkGreyText = Color.fromRGBO(22, 22, 22, 1);
  // static const whiteText = Color.fromRGBO(255, 255, 255, 0.87);
  //
  // static const white = Color.fromRGBO(255, 255, 255, 1);
  // static const purple = Color.fromRGBO(197, 209, 234, 1);
  //
  // static const green = Color.fromRGBO(3, 136, 0, 1);
  // static const red = Color.fromRGBO(234, 44, 44, 1);
  //
  //
  // static const yellow = Color.fromRGBO(225, 255, 224, 130);
}